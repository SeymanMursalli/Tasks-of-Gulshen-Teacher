#include
using namespace std;
int main()
{ int a ;
cin >> a ;
if ( a > 0) {
cout << "musbetdir" << a << endl; }
else if (a < 0) { cout << "menfi " << a << endl; }
else { cout << "0 -dır" << endl;}
return 0;
}